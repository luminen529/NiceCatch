# NEIS 글자수 계산기 구현 계획

## 1. 목표

기존 나이스 글자수 계산기의 단순한 사용성을 유지하면서, 정확성·가독성·편의성을 개선한 정적 웹사이트를 만든다.

핵심 원칙:

- 입력 즉시 결과 확인
- 2026 학교생활기록부 기준 반영
- 현재 Byte보다 "최대 Byte / 남은 Byte / 초과 Byte"를 직관적으로 표시
- 입력 내용은 서버로 전송하지 않고 브라우저에서만 처리
- 추후 기능 확장이 쉬운 구조
- 모바일과 데스크톱 모두 자연스럽게 사용 가능

---

## 2. 핵심 기능

### 글자수 계산

- 한글: 3Byte
- 영문·숫자: 1Byte
- Enter: 1Byte
- 공백 포함 글자수
- 공백 제외 글자수
- 현재 Byte 실시간 계산

Byte 계산 로직은 UI 코드와 분리하여 독립적인 유틸 함수로 구현한다.

### 항목별 제한 선택

다음 항목을 선택할 수 있도록 한다.

- 교과 세특
- 자율·자치활동
- 동아리활동
- 진로활동
- 개인별 세특
- 행동특성 및 종합의견
- 직접 설정

항목 선택 시 해당 최대 Byte가 자동 적용된다.

### 결과 표시

입력창 아래에 다음 정보를 강조해서 보여준다.

- `현재 Byte / 최대 Byte`
- 남은 Byte
- 초과 시 초과 Byte
- 사용률(%)
- 진행률 Bar
- 공백 포함 / 제외 글자수

예:

```text
1,237 / 1,500 Byte
263 Byte 남음
82.5%
```

최대 Byte를 넘으면 초과 상태가 즉시 눈에 띄도록 디자인한다.

### 입력 도구

- 전체 복사
- 전체 지우기
- 브라우저 자동 저장
- 자동 저장 ON/OFF
- 저장된 내용 삭제

자동 저장은 `localStorage`를 사용한다.

---

## 3. UI 구성

전체적으로 하나의 핵심 계산기에 집중된 단순한 레이아웃을 사용한다.

```text
Header
  로고 / 서비스명

Main
  제목
  설명 및 기준 연도

  항목 선택

  Textarea
    복사
    지우기

  Result
    현재 / 최대 Byte
    진행률
    남은 / 초과 Byte
    글자수 정보

  자동 저장 상태

Content
  NEIS Byte 계산 기준
  항목별 최대 Byte 안내
  공식 출처
  FAQ

Footer
  개인정보 안내
  비공식 서비스 안내
  업데이트 날짜
```

첫 화면에서는 계산기가 가장 먼저 보이도록 하고, 설명성 콘텐츠는 계산기 아래에 배치한다.

---

## 4. 프로젝트 구조

프레임워크를 사용하더라도 기능별 책임이 섞이지 않도록 구성한다.

```text
src/
├─ components/
│  ├─ Counter/
│  │  ├─ Counter.tsx
│  │  ├─ TextInput.tsx
│  │  ├─ ResultPanel.tsx
│  │  └─ LimitSelector.tsx
│  │
│  ├─ Info/
│  │  ├─ ByteGuide.tsx
│  │  ├─ LimitTable.tsx
│  │  └─ FAQ.tsx
│  │
│  └─ Layout/
│     ├─ Header.tsx
│     └─ Footer.tsx
│
├─ constants/
│  └─ neisLimits.ts
│
├─ hooks/
│  └─ useLocalStorage.ts
│
├─ utils/
│  └─ calculateNeisBytes.ts
│
├─ styles/
│  └─ globals.css
│
├─ App.tsx
└─ main.tsx
```

원칙:

- Byte 계산 규칙은 `utils`에서 관리
- 항목별 제한값은 `constants`에서 관리
- localStorage 로직은 `hooks`로 분리
- UI 컴포넌트에 계산 규칙을 직접 작성하지 않음
- 공식 기준 변경 시 한두 파일만 수정하면 되도록 구성

---

## 5. 데이터 구조

항목별 제한은 코드에 흩어놓지 않고 하나의 데이터 파일에서 관리한다.

예:

```ts
{
  id: "subject-detail",
  label: "교과 세특",
  maxBytes: 1500,
  description: "과목별 세부능력 및 특기사항"
}
```

추후 기준 연도가 변경되더라도 데이터만 교체할 수 있도록 한다.

---

## 6. 디자인 방향

- 학생용 도구답게 가볍고 직관적인 디자인
- 지나치게 교육기관 사이트처럼 보이지 않도록 함
- 입력창과 결과 영역을 가장 강하게 강조
- 모바일에서는 항목 선택을 가로 스크롤 또는 드롭다운으로 대응
- 성공 / 주의 / 초과 상태는 색상뿐 아니라 텍스트로도 구분
- 불필요한 애니메이션 최소화
- 빠른 로딩 우선

---

## 7. 개인정보 및 신뢰성

계산은 전부 클라이언트에서 수행한다.

명확하게 다음 내용을 표시한다.

```text
입력한 내용은 서버로 전송되지 않습니다.
자동 저장 기능 사용 시 현재 브라우저의 localStorage에만 저장됩니다.
```

또한 사이트가 NEIS, 교육부, KERIS의 공식 서비스가 아니라는 점을 하단에 명시한다.

공식 Byte 기준과 항목별 제한에는 출처와 마지막 확인 날짜를 함께 표시한다.

---

## 8. SEO

페이지 기본 메타데이터를 구성한다.

예:

```text
Title:
나이스 글자수 계산기 - 생기부 세특 Byte 계산

Description:
학교생활기록부 세특, 자율활동, 동아리활동 등 NEIS 입력 Byte를 실시간으로 계산합니다.
```

계산기 아래에는 검색 유입에 도움이 되면서 실제 사용자에게도 필요한 정보를 제공한다.

- NEIS Byte 계산 방식
- 세특 1500Byte 기준
- 항목별 글자수 제한
- 줄바꿈 Byte
- 자주 묻는 질문

---

## 9. 추후 확장

초기 버전에서는 계산기에 집중한다.

향후 후보 기능:

- 최대 Byte 초과 부분 하이라이트
- 여러 문장을 동시에 비교
- 작성 내용 임시 보관함
- 항목별 별도 계산 페이지
- 탐구 주제 추천
- 최근 이슈 / 생기부 자료 허브

확장 기능은 현재 계산기 코드에 직접 붙이지 않고 별도 모듈 또는 페이지로 추가할 수 있도록 설계한다.

---

## 10. MVP 우선순위

1. NEIS Byte 계산 로직
2. 항목별 최대 Byte 선택
3. 현재 / 최대 / 남은 / 초과 Byte 표시
4. 진행률 표시
5. 공백 포함 / 제외 글자수
6. 복사 / 초기화
7. localStorage 자동 저장
8. 반응형 UI
9. 공식 기준 및 개인정보 안내
10. SEO 메타데이터

초기 목표는 기능을 많이 넣는 것이 아니라 "빠르고 정확하며 쓰기 편한 나이스 글자수 계산기"를 완성하는 것이다.